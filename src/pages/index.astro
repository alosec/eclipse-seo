---
import Layout from '../layouts/Layout.astro';
---

<Layout title="Eclipse Central - Your Guide to Eclipses" description="Discover the wonder of eclipses with comprehensive guides, safety information, and upcoming eclipse events.">
  <section class="hero">
    <div class="hero-content">
      <h1 class="hero-title">Experience the Wonder of Eclipses</h1>
      <p class="hero-subtitle">Your complete guide to understanding, observing, and appreciating one of nature's most spectacular phenomena.</p>
      <div class="hero-actions">
        <a href="/upcoming" class="btn btn-primary">Next Eclipses</a>
        <a href="/safety" class="btn btn-secondary">Safety First</a>
      </div>
    </div>
    <div class="hero-visual">
      <div class="eclipse-animation">
        <div class="sun"></div>
        <div class="moon"></div>
      </div>
    </div>
  </section>

  <section class="features">
    <div class="feature-grid">
      <div class="feature-card">
        <h3>🌒 Types of Eclipses</h3>
        <p>Learn about solar and lunar eclipses, their differences, and what makes each one unique.</p>
        <a href="/types">Explore Types →</a>
      </div>
      <div class="feature-card">
        <h3>🥽 Safe Viewing</h3>
        <p>Essential safety guidelines for viewing eclipses without damaging your eyes.</p>
        <a href="/safety">Safety Guide →</a>
      </div>
      <div class="feature-card">
        <h3>📅 Upcoming Events</h3>
        <p>Find out when and where you can see the next eclipses from your location.</p>
        <a href="/upcoming">View Schedule →</a>
      </div>
      <div class="feature-card">
        <h3>🔬 The Science</h3>
        <p>Understand the astronomical mechanics behind these celestial alignments.</p>
        <a href="/science">Learn More →</a>
      </div>
    </div>
  </section>

  <section class="cta">
    <h2>Ready to Chase Your First Eclipse?</h2>
    <p>Join thousands of eclipse enthusiasts who trust our guides and predictions.</p>
    <a href="/upcoming" class="btn btn-primary">Find Your Eclipse</a>
  </section>
</Layout>

<style>
  .hero {
    display: grid;
    grid-template-columns: 1fr;
    gap: var(--space-8);
    padding: var(--space-16) 0;
    align-items: center;
  }

  .hero-content {
    text-align: center;
  }

  .hero-title {
    font-size: var(--text-4xl);
    font-weight: bold;
    margin-bottom: var(--space-4);
    color: var(--color-primary);
  }

  .hero-subtitle {
    font-size: var(--text-lg);
    color: var(--color-text-secondary);
    margin-bottom: var(--space-8);
    max-width: 600px;
    margin-left: auto;
    margin-right: auto;
  }

  .hero-actions {
    display: flex;
    gap: var(--space-4);
    justify-content: center;
    flex-wrap: wrap;
  }

  .btn {
    display: inline-block;
    padding: var(--space-3) var(--space-6);
    border-radius: var(--border-radius);
    text-decoration: none;
    font-weight: 600;
    transition: var(--transition);
  }

  .btn-primary {
    background: var(--color-primary);
    color: var(--color-surface);
  }

  .btn-primary:hover {
    background: var(--color-secondary);
  }

  .btn-secondary {
    background: var(--color-surface);
    color: var(--color-primary);
    border: 2px solid var(--color-primary);
  }

  .btn-secondary:hover {
    background: var(--color-primary);
    color: var(--color-surface);
  }

  .hero-visual {
    display: flex;
    justify-content: center;
    padding: var(--space-8);
  }

  .eclipse-animation {
    position: relative;
    width: 200px;
    height: 200px;
  }

  .sun {
    position: absolute;
    width: 120px;
    height: 120px;
    background: radial-gradient(circle, #FFD700, #FFA500);
    border-radius: 50%;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    box-shadow: 0 0 40px rgba(255, 215, 0, 0.5);
  }

  .moon {
    position: absolute;
    width: 100px;
    height: 100px;
    background: radial-gradient(circle, #2C2C2C, #1A1A1A);
    border-radius: 50%;
    top: 50%;
    left: 50%;
    transform: translate(-30%, -50%);
    animation: eclipse 4s ease-in-out infinite;
  }

  @keyframes eclipse {
    0%, 100% { transform: translate(-100%, -50%); }
    50% { transform: translate(20%, -50%); }
  }

  .features {
    padding: var(--space-24) 0;
    background: var(--color-surface);
  }

  .feature-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
    gap: var(--space-6);
  }

  .feature-card {
    padding: var(--space-6);
    border: 1px solid var(--color-border);
    border-radius: var(--border-radius);
    background: var(--color-background);
    transition: var(--transition);
  }

  .feature-card:hover {
    transform: translateY(-4px);
    box-shadow: 0 8px 24px rgba(0, 0, 0, 0.1);
  }

  .feature-card h3 {
    font-size: var(--text-xl);
    margin-bottom: var(--space-3);
    color: var(--color-primary);
  }

  .feature-card p {
    color: var(--color-text-secondary);
    margin-bottom: var(--space-4);
  }

  .feature-card a {
    color: var(--color-accent);
    text-decoration: none;
    font-weight: 600;
  }

  .cta {
    padding: var(--space-24) 0;
    text-align: center;
  }

  .cta h2 {
    font-size: var(--text-3xl);
    margin-bottom: var(--space-4);
    color: var(--color-primary);
  }

  .cta p {
    font-size: var(--text-lg);
    color: var(--color-text-secondary);
    margin-bottom: var(--space-8);
  }

  @media (min-width: 768px) {
    .hero {
      grid-template-columns: 1fr 1fr;
      text-align: left;
    }

    .hero-content {
      text-align: left;
    }

    .hero-actions {
      justify-content: flex-start;
    }

    .hero-title {
      font-size: clamp(var(--text-3xl), 5vw, 4rem);
    }
  }
</style>
